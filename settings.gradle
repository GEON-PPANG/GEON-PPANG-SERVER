rootProject.name = 'GUN-PPANG-SERVER-V1' 

include(
    "common:enum",
    "common:util",
    "common:domain",
    "client:client-amplitude",
    "client:client-apple",
    "api",
    "storage:db-core",
    "support:slack",
)

