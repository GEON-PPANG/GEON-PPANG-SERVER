rootProject.name = 'gunbbang'

include(
    "common:enum",
    "common:util",
    "common:domain",
    "common:exception",
    "client:client-feign",
    "api",
    "client:client-feign",
    "storage:db-core",
)

